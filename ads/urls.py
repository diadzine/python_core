from django.conf.urls import patterns, url

from ads import views

urlpatterns = patterns(
    'ads.views',
    # url(
    # regex=r'^$',
    # view='get',
    # name='basic_urls',
    # ),

    # url(
    #     regex=r'^ads/$',
    #     view='get',
    #     name='basic_urls',
    # ),

    # url(
    #     regex=r'^save/$',
    #     view='save',
    #     name='basic_urls',
    # ),

    # url(
    #     regex=r'^delete/$',
    #     view='delete',
    #     name='basic_urls',
    # ),

)
